# Exclude everything by default from build context
*

# Keep only the packaged Quarkus JVM artifact layout used by src/main/docker/Dockerfile
!target/
target/*
!target/quarkus-app/
!target/quarkus-app/**
