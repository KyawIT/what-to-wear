FROM node:20-alpine

WORKDIR /app

COPY package.json ./
COPY scripts/ ./scripts/
RUN npm install --legacy-peer-deps --no-audit --no-fund

COPY . .

EXPOSE 8081

CMD ["npx", "expo", "start", "--web", "--non-interactive", "--port", "8081", "--host", "0.0.0.0"]
