import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  selectPhotoText: {
    fontFamily: "Inter_400Regular",
    fontSize: 15,
    color: "#9B8B7F",
    marginTop: 12,
  },
});
