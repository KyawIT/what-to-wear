# FastAPI and server dependencies
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Image processing
pillow==11.1.0

# Vector database
qdrant-client==1.16.2

# Data processing
pandas==2.2.3
numpy>=1.24.0,<2.0.0
scikit-learn>=1.0.0

# Progress bars
tqdm==4.67.1

# NOTE: Heavy ML deps (torch, torchvision, sentence-transformers) are in
# requirements-ml.txt and installed as a separate Docker layer for caching.
