# Use CPU-only PyTorch (much smaller image, sufficient for embedding inference)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchvision==0.20.1+cpu

# ML and image processing
pillow>=10.0.0
numpy>=1.24.0
opencv-python>=4.8.0
sentence-transformers==3.3.1

# Vector database
qdrant-client>=1.16.0

# Data processing
pandas>=2.0.0

# Progress bars
tqdm>=4.65.0
